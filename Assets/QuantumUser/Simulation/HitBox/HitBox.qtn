component HitBox
{
    AssetRef<HitBoxConfig> CurrentHitBox;
    bool IsActive;
    EntityRef Owner;  // Who created this hitbox (for damage attribution)
    FP ActiveTime;   // When this hitbox became active (for duration tracking)
    array<entity_ref>[8] HitEntities; // Track entities hit by this hitbox instance
}